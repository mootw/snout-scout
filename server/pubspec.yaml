name: server
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  snout_db:
    path: ../snout_db

  rfc_6901: ^0.1.1

  shelf: ^1.4.0
  shelf_web_socket: ^1.0.3

dev_dependencies:
